<%= form_for @accounting, :html => { :multipart => true } do |f| %>
<% if @accounting.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@accounting.errors.count, "error") %> 入力内容が適切ではありません。　</h2>
    <ul>
    <% @accounting.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
     <% end %>
     </ul>
    </div>
    <% end %>

    <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
    </P>
     <p>
      <%= f.label :name %><br>
      <%= f.text_field :name %>
                </p>
     <p>
     <%= f.label :application_date %><br>
     <%= f.datetime_select(:application_date, use_month_numbers: true)%>
     </p>
    <p>
        <%= f.label :purpose %><br>
         <%= f.text_area :purpose %>
     </p>
     <!--
    <p>
      <% if params[:action]=="edit" then %>
        <%= f.label :status %><br> 
       <%= f.radio_button :status, "1" %> 申請中
       <%= f.radio_button :status, "2" %> 承認
       <%= f.radio_button :status, "3" %> 却下
      <% end %>
    </p>
    -->
    
   <%= f.fields_for :images do |imf| %>
       <%= imf.file_field :image_file %>       
    <% end %>
 <p>
 <%= f.submit %>
 </p>
 <% end %>

