<h2><%= @project.title %></h2>
<p>
  <strong>Title:</strong>
  <%= @task.title %>
</p>
 
<p>
  <strong>Due:</strong>
  <%= @task.due %>
</p>

<p>
  <strong>user:</strong>
  <% @assignees.each do |a| %>
    <%= a.name %>, 
  <% end %>
</p>

<p>
  <strong>Content:</strong>
  <%= @task.content %>
</p>
<%= link_to 'プロジェクトページに戻る', project_path(@project) %>
| <%= link_to 'タスク内容を編集する', edit_project_task_path(@project, @task) %>
